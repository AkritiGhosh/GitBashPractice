git clone : To create a local copy of the repository from Github

git add : To add a new file into git

git commit -m "commit_statement"  : To save the changes in the file

git push -u origin master : To push/upload the file from local system to the Github repository.